# Markdown Printer 1.0.4

Direct PDF dragging now works with destinations such as Microsoft Teams that require concrete local file URLs.

- Materializes a real local PDF when a drag begins.
- Retains the original Markdown filename, changing only the final extension to `.pdf`.

This universal macOS 14+ build is signed with Developer ID and notarized by Apple.
